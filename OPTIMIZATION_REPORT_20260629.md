# 四模式交易機器人檢討與修正執行方案

日期：2026-06-29

環境：OKX Demo

程式版本：`structure-risk-v6-20260628`

## 1. 結論

目前 v6 尚未形成可持續正期望值。最近 100 筆 OKX 唯一平倉中，85 筆可對應 v6：

- 勝 44、負 41，勝率 51.8%。
- 淨損益 -199.1751U。
- 純價格損益 -177.9657U。
- 手續費 -121.1175U。
- 資金費 +99.9081U，正資金費掩蓋了部分方向虧損。
- PF 0.563。
- 平均獲利 5.839U，平均虧損 -11.124U。
- 8 筆價格方向正確，但扣費後變成虧損。
- 33 筆持倉少於 10 分鐘，56 筆少於 30 分鐘。
- 67 筆空單、18 筆多單，方向明顯偏空。

所以問題不是單純勝率不足，而是虧損幅度約為獲利幅度的 1.9 倍，加上交易過密與費用侵蝕。

## 2. 目前持倉

檢查時共有 4 個 Demo 淨倉，浮動損益約 -6.8U：

| 標的 | 方向 | 本機策略 | 浮盈虧 | 狀態 |
|---|---:|---|---:|---|
| ALGO-USDT-SWAP | 空 | SqueezeHunter | +1.45U | 已推成本安全停損 |
| AVAX-USDT-SWAP | 多 | Contrarian | -5.64U | 有止盈止損 |
| USDC-USDT-SWAP | 空 | Mixed | 約 -0.1U | 兩組保護單 |
| XAU-USDT-SWAP | 多 | Mixed | 約 -2.0U | 兩組保護單，另有舊反向減倉單 |

四個淨倉的停損尺寸均有覆蓋，限價止盈尺寸也覆蓋目前倉位，但 XAU 存在一筆舊方向的 reduce-only 掛單，顯示孤兒掛單清理不完整。

## 3. 高優先級缺陷

### P0：績效歸因錯誤

程式只用 `posId` 對應策略，但 OKX 可能重複使用同一淨倉 ID；同幣多模式合併後又標成 `Mixed`。最近 85 筆 v6 平倉中有 27 筆 Mixed，合計 -95.1275U，完全沒有進入四模式 optimizer。

介面顯示 MeanReversion PF 1.853、+8.09U，但使用時間與 posId 重新核對後，MeanReversion 相關 27 筆實際約 -39.62U。現有自動優化因此會學到錯誤結果。

### P0：四執行緒存在同幣競態

每個模式各自擁有 cooldown 與 executed signal 記錄，沒有全域 symbol reservation。兩個模式可在同一時間看見「尚無倉位」並同時成交，造成 Mixed 淨倉、重複保護單與風險累加。

KAITO 淨倉最終 -97.44U，遠高於單筆規劃的 18U，符合跨模式／加倉後按單筆風險估算失真的特徵。

### P0：交易池混入錯誤資產類型

掃描器只檢查 USDT SWAP，沒有排除股票代幣、金屬與穩定幣：

- PLTR -33.27U。
- NVDA 兩筆合計約 -11.30U。
- XAG -10.94U。
- USDC 價格獲利約 +0.73U，但費用與資金費後為 -3.38U。

股票代幣有交易時段與跳空特性，金屬和穩定幣也不適合直接套用加密幣的 ATR、資金費與 24/7 交易假設。

### P0：18U 只是估算，不是淨倉硬限制

目前用入場時點差推算 stop slippage，且上限只估到 0.6%。實際跳空、深度變化、跨模式加倉與淨倉共用停損可使損失遠超預算。v6 至少有 5 筆虧損超過 18U。

## 4. 策略與出場問題

### MacroSniper

- 相關 14 筆唯一歸因約 -32.27U。
- 趨勢策略在 ranging 狀態仍大量占用交易；總體 79% 為空單。
- 短線回吐與費用使小幅正確方向無法轉成淨利。

### MeanReversion

- 相關 27 筆約 -39.62U，但純價格損益接近損益兩平；主要問題是約 -37.59U 費用。
- 這是目前最值得保留的模式，但必須降低短時間重複交易並以淨獲利空間判斷是否值得入場。

### Contrarian

- 相關 10 筆約 +56.47U，但其中約 +54.01U 來自資金費。
- 純價格優勢約 +12.68U，樣本太少，不能依資金費意外收入放大信心。

### SqueezeHunter

- 相關 7 筆約 -88.62U，是目前最弱模式。
- PLTR、SPK 等大幅跳空／突破失敗單顯示突破後追價與資產分類錯誤同時存在。
- 在修正前不應根據現有成績放大倉位。

## 5. 其他程式問題

1. API 金鑰、Secret、Passphrase 直接寫在 `server.py`，應改為環境變數或唯讀設定檔並輪換現有金鑰。
2. 多處 `except Exception: pass` 會吞掉餘額、同步、保護單和寫檔錯誤。
3. JSON 日誌不是交易生命週期資料庫，無唯一事件鍵、不可安全重放。
4. Mixed 倉位一律套用 SqueezeHunter 的 runner 規則，與實際組成策略無關。
5. UI 的 SL 只顯示其中一張保護單；XAU 目前兩個尺寸的停損價不同，畫面無法完整表達。
6. 資金進度以 `USDT equity - 5000` 計算，會把人工增加 Demo 資金誤當策略獲利。
7. 資金費被納入訊號績效，會把 carry 收益誤認成方向 alpha。
8. 目前服務沒有輸出到最新日誌檔，執行錯誤和保護單異常不易追蹤。
9. 現有測試只有 4 項，沒有跨模式競態、交易生命週期、部分平倉、孤兒掛單、滑價與回放測試。

## 6. 修正執行方案

### 第一階段：先修資料真相

1. 建立不可重複的 `trade_lifecycle_id`，保存 entry order、fill、algo、position、strategy、version、時間與名義金額。
2. 以 `(posId, cTime, uTime, orderId)` 對帳，不再只用 posId。
3. Mixed 淨倉按每個模式的實際成交名義金額分攤價格 PnL、費用與資金費。
4. 分開顯示 `price_pnl`、`fees`、`funding`、`net_pnl`。
5. 資金曲線依帳務流水扣除人工入金／轉帳。

驗收：UI 最近 100 筆淨損益、費用與資金費逐項對上 OKX，誤差小於 0.01U；未歸因與重複交易為 0。

### 第二階段：修淨倉與執行安全

1. 新增全域 symbol lock 與 reservation，四模式可同時掃描，但同一淨倉的檢查、下單、建保護單必須原子化。
2. 風險以整個淨倉計算，不以單筆 entry 計算；新增 `planned_net_risk_u` 與 `actual_exit_slippage_u`。
3. 依 order-book depth 計算最大名義金額；點差與深度惡化時降低槓桿，不用 stop-limit 冒未成交風險。
4. 排除股票代幣、金屬、外匯、穩定幣永續；若要交易，另建專用模式與交易時段。
5. 每輪對帳並取消已無對應倉位的 reduce-only 孤兒單。

驗收：無 Mixed 競態加倉；實際單次淨倉損失不超過規劃風險 1.5 倍；所有倉位 TP/SL 尺寸總和等於淨倉尺寸。

### 第三階段：重構四模式

1. Regime allocator：ranging 優先 MeanReversion、trending 優先 MacroSniper、expansion 優先 SqueezeHunter、exhaustion 優先 Contrarian。
2. 所有模式仍可掃描與產生樣本，但非優勢 regime 維持 60U 基準且降低槓桿，不靠停掉模式製造漂亮績效。
3. Macro 必須收線突破後回測結構位，禁止遠離 EMA／突破位追價。
4. Mean 以 BB/VWAP/均值距離和預估淨獲利空間入場，避免穩定幣與低波動費用單。
5. Contrarian 要求 sweep/divergence 後 reclaim，信心只看價格 alpha，不使用資金費。
6. Squeeze 要求收線突破、成交量、ADX、邊界回測與深度確認；先重新收集 30 筆 60U 樣本。

### 第四階段：重做止盈止損與 runner

1. 初始 SL 放在結構失效點，槓桿反推可承受 U 損失。
2. 當浮盈足以覆蓋預估來回費用與滑價後才推成本安全停損。
3. 先在 0.5R 至 0.8R 或均值／結構目標部分止盈，其餘部位用 swing/ATR runner。
4. runner 只能修改該 lifecycle 綁定的 algoId，不能按幣種和方向批次修改所有保護單。
5. 實際滑價超過模型 1.5 倍時，降低該標的名義金額並重新估算深度。

### 第五階段：重做自動學習與放大

1. 每模式至少 30 筆唯一、已對帳、扣費後樣本才允許放大。
2. 放大條件：rolling PF >= 1.20、expectancy > 0、平均盈虧比 >= 0.9、最大連虧可接受、fee-flip < 5%。
3. 60U -> 75U -> 90U -> 120U 分級放大；任一條件失效立即退回前一級。
4. 資金費獨立為 carry 指標，不加入方向信心。

### 第六階段：UI 與驗證

UI 必須顯示版本、lifecycle、模式、入場依據、實際成交、結構位、TP/SL 各張尺寸、規劃風險 U、MFE/MAE、價格 PnL、費用、資金費、淨 PnL、optimizer 使用樣本與下一步動作。

新增至少以下測試：

- 四模式收線訊號與無未來函數回放。
- 同幣四執行緒競態。
- 加倉、部分平倉、反手與 posId 重用。
- OCO 建立、amend、孤兒單清理與尺寸對帳。
- 點差、深度、跳空與風險倍數。
- 入金調整後資金曲線。

## 7. 上線門檻

修正後仍先使用 Demo 與 60U 基準。每模式累積至少 30 筆唯一平倉，且整體同時滿足：

- PF >= 1.15。
- 扣費後 expectancy > 0。
- 平均盈虧比 >= 0.9。
- fee-flip < 5%。
- 實際最大單筆損失 <= 規劃風險 1.5 倍。
- OKX、資料庫與 UI 對帳誤差 < 0.01U。

未達門檻時不得宣稱可以把 5000U 穩定變成 10000U，也不得因短期資金費或單筆大賺自動放大。
